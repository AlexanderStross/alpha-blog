<%= link_to((category.name + " " + content_tag(:span, category.article_count, :class => "badge bg-primary shadow rounded-pill")).html_safe, category_path(category.id), class: "btn btn-secondary shadow btn-sm mb-1") %>
